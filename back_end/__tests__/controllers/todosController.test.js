import assert from 'assert';

describe('todosController', () => {
    test('hello world!', () => {
        assert.strictEqual(1 + 1, 2);
    });

    test('hello world!', () => {
        assert.strictEqual(1 + 1, 2);
    });

    test('hello world!', () => {
        assert.strictEqual(1 + 1, 2);
    });

    test('hello world!', () => {
        assert.strictEqual(1 + 1, 2);
    });

});